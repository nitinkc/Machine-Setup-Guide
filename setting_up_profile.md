# setting up .profile

###Setting up the Env variables.

Go to your home (~) directory

`cd ~` or `cd /user/<user_name>`

`touch .profile`

`touch .bash_profile`

`touch .bash_aliases`

`touch .bashrc`



